---
maintenance:
  children:
    tags_maintenance:

semaphore:
  children:
    tags_semaphore:

proxmox:
  children:
    tags_proxmox:

proxmox_coresvc:
  children:
    tags_proxmox_coresvc:

loadbalancer:
  hosts:
    tags_loadbalancer:
